---
type: environment
env: dependent
---

# environment.md — Current machine

Free-form. Loaded at boot. Not part of the registry.

- **Machine:** main-pc · Windows · user `tdsnit`
- **Vault:** `C:\Users\tdsnit\Documents\Obsidian Vault\` (symlink: `C:\Users\tdsnit\winlinks\obsidian-default-vault`)
- **Primary harness:** Claude Desktop. Cowork and CC CLI also available.
- **MCP servers active:** `filesystem`, `aae-mcp` (in build, replacing `vault-mcp`).
- **v3 ecosystem:** `2 Agents/`. v2 at `2 AI Exchange/` still active during transition.

For paths, repos, MCPs, and enablers in detail see `registry/`.
