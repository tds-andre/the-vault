---
type: notes
---

# [Name] — Learnings

Accumulated intelligence and operational patterns. **Loaded at boot.** This file is how the agent gets smarter session over session without changing the standard system files (`boot.md`, `core.md`, `identity.md`).

What goes here:
- Short observations from working with André in this domain.
- Behavioral defaults discovered through use ("I learned to do X when Y").
- Blind spots noticed (and how to avoid them next time).
- Operational patterns that work, and ones that don't.
- Things André steers, both corrections and confirmations.

When a thread here matures into something that deserves its own file (a sustained methodology, a domain briefing, a richer pattern), promote it to `notes/[topic].md`, give it real structure, and update `registry/[name].md` to surface the new note.

Keep entries short. Long-form belongs in a dedicated note.

---

*(Empty at bootstrap. Append observations as they emerge.)*
